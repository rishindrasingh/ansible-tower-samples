---
- name: Copy each file over that matches the given pattern
  host: all
  become: yes
  copy:
    src: "{{ item }}"
    dest: "/opt/test/dist"
    owner: "root"
    mode: 0600
  with_fileglob:
    - "/opt/test/src/a-*"